import { createStyles } from "@material-ui/core/styles";

export const styles = () => createStyles({
    PaperGrid: {
        width: '40%',
        marginTop: '1%',
        overflowX: 'auto',
        marginLeft: '30%',
        marginRight: '30%',
    },
    TableGrid: {
        minWidth: '300',
    }
});